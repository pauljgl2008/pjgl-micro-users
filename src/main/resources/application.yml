spring:
  application:
    name: users
  profiles:
    active: local
  config:
    import: optional:configserver:http://localhost:8085
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    bus:
      enabled: true
      refresh:
        enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  security:
    enabled: false
